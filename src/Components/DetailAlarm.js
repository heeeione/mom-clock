import { Box, Modal } from '@mui/material';

const DetailAlarm = ({ open, onClose }) => {
  return (<Modal open={open} onClose={onClose}>
    <Box>h1</Box>
  </Modal>);
};

export default DetailAlarm;
